<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Button themes -->
    <style name="Theme.Syncthing.GreyButton" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:textColor">@color/btn_welcome_slide_color</item>
        <item name="backgroundTint">@color/btn_welcome_slide_background</item>
        <item name="strokeColor">@color/btn_welcome_slide_stroke</item>
        <item name="strokeWidth">1dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Dialog themes -->
    <eat-comment/>

    <style name="Theme.Syncthing.Dialog" parent="ThemeOverlay.Material3.Dialog.Alert">
        <item name="colorPrimary">@color/primary_dark</item>
    </style>
</resources>
